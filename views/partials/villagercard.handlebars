
          {{#each villagers}}
            <div class="Villager-card">
                <img id="villager-image" src={{image}} alt="Villager-picture" style="width:100%">
                <ul id="villager-text">
                    <li>Name:{{name}}</li>
                    <li>Personality: {{personality}}</li>
                    <li>Species: {{animal}}</li>
                                <div class="hiddenVill">

             <form id="updateVills" action="updateVills/" method="post">
             Villager Name: <input type="text" name="ffvillager" value = {{name}} readonly="readonly"><br>
             New Image: <input type="text" name="fimageVill" value = ""><br>
        New Personality: <input type="text" name="fpersonality" value = {{personality}}><br>
        New Species: <input type="text" name="fanimal" value = {{animal}}><br>

        <input type="submit" value= "Submit">
        </form>
        </div>
                </ul>
                <div style="clear:both"></div>
            </div>
            {{/each}}
